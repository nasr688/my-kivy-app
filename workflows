name: Build APK
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: Artemix/buildozer-action@v1
      with:
        buildozer_version: stable
    - uses: actions/upload-artifact@v4
      with:
        name: package
        path: bin/*.apk
